/*
Nível Médio
Crie um programa que exiba para o usuário qual é a área de um retângulo.
Para implementar esse programa você deverá seguir as seguintes regras:
•	O usuário deverá informar para o programa o valor da base e altura do retângulo/quadrado.
•	É obrigatório criar/utilizar uma classe para representar o retângulo/quadrado.
•	A base e a altura informada pelo usuário deveram ser representadas na classe como propriedades.
•	O objeto deverá possuir uma propriedade para exibir o valor da área do retângulo/quadrado.
•	O objeto deverá possuir um método que exiba os dados de todas as suas propriedades.
*/

using System;

class Retangulo
{
    protected double Base { get; set; }
    protected double Altura { get; set; }

    protected double Area
    {
        get { return Base * Altura; }
    }

    protected void Dados()
    {
        Console.WriteLine("Dados:");
        Console.WriteLine($"Base: {Base}");
        Console.WriteLine($"Altura: {Altura}");
        Console.WriteLine($"Area: {Area}");
    }
}

class RetanguloMain : Retangulo
{
    internal void Executar(double b, double h)
    {
        Base = b;
        Altura = h;
        Dados();
    }
}

class Program
{
    static void Main()
    {
        Console.WriteLine("Base:");
        double baseInp = double.Parse(Console.ReadLine());

        Console.WriteLine("Altura:");
        double alturaInp = double.Parse(Console.ReadLine());

        RetanguloMain ret = new RetanguloMain();
        ret.Executar(baseInp, alturaInp);
    }
}
